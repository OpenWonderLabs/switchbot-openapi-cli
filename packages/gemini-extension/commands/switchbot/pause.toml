description = "Pause a moving SwitchBot device (Curtain, Robot Vacuum, etc.)"
prompt = """
Pause the SwitchBot device the user names (stops movement for Curtains, Robot Vacuums, etc.).
Resolve the name to a deviceId (alias → exact → prefix → substring → fuzzy). Verify the device
supports "pause" via describe_device. This is a mutation-tier command. Use the send_command MCP
tool with command "pause". Report the result.
User input: {{args}}
"""
