description = "Press a SwitchBot Bot button (momentary tap)"
prompt = """
Press the SwitchBot Bot the user names (momentary button tap). Resolve the name to a deviceId
(alias → exact → prefix → substring → fuzzy). Verify the device supports the "press" command
via describe_device. This is a mutation-tier command. Use the send_command MCP tool with
command "press". Report that the button was pressed.
User input: {{args}}
"""
