description = "Open a SwitchBot Curtain, Blind Tilt, or Roller Shade"
prompt = """
Open the SwitchBot Curtain, Blind Tilt, or Roller Shade the user names. Resolve the name to a
deviceId (alias → exact → prefix → substring → fuzzy). Verify the device supports "open" or
"setPosition" via describe_device. This is a mutation-tier command. Use the send_command MCP
tool — prefer setPosition with parameter "0" (fully open) if supported, otherwise use command
"open". Report the result.
User input: {{args}}
"""
